package lib.ds;

/**
 * Wavelet Matrix。静的数列に対する順位・頻度・k番目・前後値クエリを O(log sigma) で提供する。
 */
@SuppressWarnings("unused")
public final class WaveletMatrix {
	// TODO: succinct bit vectorを用いた int / long 版の構築と rank / quantile / rangeFreq を実装する
}
