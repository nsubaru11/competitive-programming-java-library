package lib.ds;

/**
 * Sliding Window Aggregation（SWAG）。モノイド上のキュー全体の集約値を償却 O(1) で取得する。
 */
@SuppressWarnings("unused")
public final class SlidingWindowAggregation {
	// TODO: 2スタックによる generic / int / long 版の push / pop / fold を実装する
}
