/**
 * Sparse Table。冪等な演算（min / max / gcd 等）の区間クエリを前計算 O(N log N)・クエリ O(1) で提供する。
 */
@SuppressWarnings("unused")
public final class SparseTable {
	// TODO: Sparse Table（int / long 特化版とジェネリクス版、構築とO(1)区間クエリ）の実装を行う
}
