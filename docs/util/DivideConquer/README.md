# DivideConquer（🚧 一部未実装）

## 概要

分割統治法を用いる汎用アルゴリズムを配置するためのクラスです。

## 実装クラス

### [DivideConquer](../../../src/lib/util/DivideConquer.java)

- `inversionCount(int[] a, int n)` が宣言されています
- 現在の本体は未完成で、入力にかかわらず `0` を返します
- 正しい転倒数計算として利用できません

## 注意事項

実装完了まではこのクラスを利用せず、マージソートまたはBITによる転倒数計算を個別に用意してください。
