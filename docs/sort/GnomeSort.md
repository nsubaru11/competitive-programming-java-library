# GnomeSort

`basicGnomeSort(int[])`と`gnomeSort(int[])`は、隣接要素を交換しながら前後へ移動する昇順のノームソートです。どちらも入力配列を直接変更し、安定で、最悪時間計算量は`O(N^2)`、追加領域は`O(1)`です。

`gnomeSort`は、確定済みの先頭位置を記録して不要な再走査を抑える版です。実戦向けではなく、挿入ソートと近い性質を持つ実装例として使用してください。
