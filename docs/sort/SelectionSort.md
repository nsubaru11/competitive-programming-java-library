# SelectionSort

`selectionSort(int[])`は未整列部分から最小値を選び、先頭と交換する昇順の選択ソートです。`doubleSelectionSort(int[])`は最小値と最大値を同時に選び、両端へ配置します。いずれも入力配列を直接変更し、追加領域は`O(1)`、時間計算量は`O(N^2)`です。安定ソートではありません。

実戦での単一配列ソートには`Arrays.sort`を優先し、交換回数や双方向選択の挙動を確認したい場合に使用してください。
